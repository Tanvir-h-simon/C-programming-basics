#include <stdio.h>
int main()
{
    int length = 17;
    int width = 21;
    int area = length*width; //area of a rectangular = length*width


    printf("Length of the rectangular is: %i\n", length);
    printf("Width of the rectangular is: %i\n", width);
    printf("Area of the rectangular is: %i", area);
    return 0;
}
